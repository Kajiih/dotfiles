# Automatically loaded after the secret manager's secret
# For secrets not managed by the secret manager or in case you can't use it

load-env {
    # SOME_SECRET: VALUE
    # OTHER_SECRET: VALUE
    }
